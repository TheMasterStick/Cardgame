# Board backgrounds

Drop background images here, then point to them from
`src/data/theme.ts` (`appBackground`, `opponentBoardBackground`,
`playerBoardBackground`) as `"/boards/your-file.jpg"`.

External URLs work too. Leave a field as `""` to keep the plain dark
background.
